/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "source.unsplash.com",
      "images.unsplash.com",
      "picsum.photos",
      "localhost",
      "plus.unsplash.com",
      "res.cloudinary.com"
    ],
  },
};

export default nextConfig;
